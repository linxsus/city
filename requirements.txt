# Automation Framework - Dépendances Python
# Installation: pip install -r requirements.txt

# =====================================================
# PyTorch - Choisir UNE option selon votre configuration
# =====================================================
# Option CPU (plus léger, pas besoin de GPU):
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# Option GPU CUDA 12.1 (nécessite GPU NVIDIA compatible):
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# =====================================================
# Dépendances principales
# =====================================================

# Capture d'écran
mss>=9.0.0

# Vision par ordinateur
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# Automatisation
pyautogui>=0.9.54

# Windows (Windows uniquement)
pywin32>=306; sys_platform == 'win32'
pynput>=1.7.6

# =====================================================
# OCR - Choisir UNE option
# =====================================================
# Option 1: EasyOCR (recommandé, supporte GPU, nécessite PyTorch)
easyocr>=1.7.0

# Option 2: Tesseract (léger, CPU only)
# Installer Tesseract séparément: https://github.com/UB-Mannheim/tesseract/wiki
# pytesseract>=0.3.10

# =====================================================
# Dépendances de développement / Tests
# =====================================================
pytest>=7.4.0
pytest-cov>=4.1.0
